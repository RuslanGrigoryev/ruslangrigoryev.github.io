define(function(require) {
	var $ = require('jquery'),
		_ = require('underscore'),
		Backbone = require('backbone'),
		Marionette = require('marionette'),
		ModalView = require('block!modal');

	var ModalReputationView = ModalView.extend({
	});

	return ModalReputationView;
});